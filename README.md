# Invoice-Generator
The project includes software to generate invoice. The provided code is meant for a restaurant owner, however one can change the contents depending upon the need of the user.
This project is made using Python only, but to increase the efficiency on a large level, one can use modes such as Structured Query Language at the backend.
